server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Кэш статических ресурсов с хэшами
  location ~* \.(?:js|css|png|jpg|jpeg|gif|svg|ico|webp|avif|woff2?)$ {
    access_log off;
    add_header Cache-Control "public, max-age=31536000, immutable";
    try_files $uri =404;
  }

  # SPA fallback
  location / {
    try_files $uri /index.html;
  }
}
